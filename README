Sending Growl notifications from Last.fm website. To install make sure you have a the greasemonkey plugin installed, then click on the .user.js file and then click on the raw file, it should install from there